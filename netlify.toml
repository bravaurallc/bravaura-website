# Bravaura LLC — static site, no build step.
# Everything in this folder is published as-is.
[build]
  publish = "."

# The site is generated by _build/build_site.py on Kendal's machine,
# so Netlify does not need to run anything.
